[package]
name = "synapse-meta-tracing"
description = "Flexible metadata collection for ETL processing"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = {workspace = true, features = ["derive"]}
serde_json = {workspace = true}
tracing = {workspace = true}
